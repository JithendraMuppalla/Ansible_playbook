---
- hosts: all
  connection: ssh
  remote_user: ec2-user
  gather_facts: no
  tasks:
    - name: upgrade all packages
      yum:
       name: '*'
       state: latest

- name: 4. install Apache
      yum:
        name: git
        state: latest
        install_recommends: yes


